// Package plugin provides abstractions for commands and modules and all types
// associated with them.
package plugin

// BuiltInSource is the name of the built-in plugin source.
const BuiltInSource = "built_in"
